Library-bootstrap is a bootstrap repository to quickly start a new PHP library.

Getting started
===============

1. Clone the repository.

    `git clone git@github.com:fieg/library-bootstrap.git <your-library-dirname>`

2. Call make.

    `make`
